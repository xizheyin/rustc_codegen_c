[package]
name = "rgenc"
version = "0.1.0"
edition = "2021"

[lib]
name = "rgenc"
crate-type = ["dylib"]

[dependencies]
itertools = "0.13"
rust2cast = { path = "../rust2cast" }


[package.metadata.rust-analyzer]
rustc_private = true
